.venv/
.pysite/
__pycache__/
*.egg-info/
*.pyc
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
# Algorithm/session work-tracking artifacts — contain machine-local info; never commit
MEMORY/
# Brainstorming / writing-plans process artifacts — not project documentation;
# the durable record lives in docs/adr/ and docs/decisions/.
docs/superpowers/
notes-mvp/opencode.json
notes-mvp/.env
notes-mvp/sample-notes/documents/
# Install-root runtime artifacts (normally outside the repo; safety-net if INSTALL_ROOT points here)
oc-home/
notes.git/
workspace/
.sweep-state.json
# Runtime proposal staging file — written to notes_root/inbox/ by the propose MCP tool.
# Also created in the project root by tests using tmp_path-based notes roots.
inbox/
# Generated OpenCode config / credentials — may carry machine-local values or a
# real API key; never commit, regardless of where they land in the tree.
**/opencode.json
**/auth.json
# Local audit outputs — durable offline review but not project documentation.
findings.md
pr-audit-findings.md
# Per-session algorithm work-tracking artifacts. The durable record lives
# in docs/adr/ and docs/decisions/; the session trees under WORK/ are
# machine-local scratch and conversation.txt is the per-turn transcript
# of the live agent (operation log).
WORK/
conversation.txt
